# Docker-specific configuration
server.port=8081

# MySQL in Docker
spring.datasource.url=jdbc:mysql://mysql:3306/chatbot_db?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Addf

# Emotion Service in Docker
emotion.service.url=http://emotion-service:5000

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect